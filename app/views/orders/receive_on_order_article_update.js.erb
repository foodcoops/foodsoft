// Handle more advanced DOM update after AJAX database manipulation.
// See publish/subscribe design pattern in /doc.
(function(w) {
  $('table#order_articles').parents('form').receiveOrderForm('replace', {
    id: <%= @order_article.id %>,
    newEntry: $('<%= j render(partial: 'edit_amount', locals: {order_article: @order_article}) %>')
  });
})(window);

