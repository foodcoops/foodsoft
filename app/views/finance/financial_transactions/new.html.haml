- title t('.title')

%p!= t('.paragraph', name: @ordergroup.name)

= simple_form_for @financial_transaction, url: finance_ordergroup_transactions_path(@ordergroup), validate: true do |f|
  = f.hidden_field :ordergroup_id
  = f.input :amount
  = f.input :note, :as => :text
  = simple_form_submit(f)
