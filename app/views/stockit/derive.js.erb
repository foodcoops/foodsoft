$('#modalContainer').html('<%= j(render(
  :partial => "form",
  :locals => {
    :title => t('.title'),
    :stock_article => @stock_article
  }
)) %>');

new bootstrap.Modal('#modalContainer').show();
