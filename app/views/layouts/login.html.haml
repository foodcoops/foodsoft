= render layout: 'layouts/header' do
  .container
    .row
      .offset-md-3.col-md-6
        = bootstrap_flash_patched
        - if show_title?
          %h1= yield(:title)
        = yield

        = render 'layouts/footer'
