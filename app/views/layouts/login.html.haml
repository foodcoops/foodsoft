= render layout: 'layouts/header' do
  .container
    .row
      .span6.offset3
        = bootstrap_flash_patched
        - if show_title?
          .page-header
            %h1= yield(:title)
        = yield

        = render 'layouts/footer'
