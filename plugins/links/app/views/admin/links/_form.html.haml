= simple_form_for [:admin, @link], validate: true, remote: true do |f|
  .modal-dialog
    .modal-content
      .modal-header
        %h3.modal-title= @link.new_record? ? t('.title_new') : t('.title_edit')
        = close_button :modal
      .modal-body
        = f.input :name
        = f.input :url
        = f.association :workgroup
        = f.input :indirect, input_html: { 'data-bs-toggle' => 'collapse', 'data-bs-target' => '#authorization' }
        #authorization{class: @link.indirect? ? '' : 'collapse'}
          = f.input :authorization
        %small= t '.description'
      .modal-footer
        = link_to t('ui.close'), '#', class: 'btn btn-secondary', data: {'bs-dismiss': 'modal'}
        = f.submit class: 'btn btn-primary'
