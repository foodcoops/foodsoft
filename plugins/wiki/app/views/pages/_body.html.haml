- content = wikified_body @page.body, @page.title
- toc = generate_toc @page.body

- unless toc.blank? || params[:preview]
  - content_for :sidebar do
    #wikitoc.card.bg-light
      .card-body
        %h5.card-title= t '.title_toc'
        = toc
= content
